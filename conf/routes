# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Trip API Endpoints
GET        /api/trips           controllers.ApiController.getTrips
GET        /api/weekends        controllers.ApiController.getWeekends

# Airport routes
GET     /api/airports     controllers.AirportController.getAllAirports()

# Health Endpoints
GET     /health           controllers.HealthController.health

